# VoxelVerse Studio

Placeholder only. Do not implement Studio here yet.

Studio will eventually be a Tauri + React app with a Rust backend that uses the same crates as the game:

- `vv-content-schema`
- `vv-pack-loader`
- `vv-pack-compiler`
- future `vv-preview-render`
- future `vv-texture-gen`

No independent pack parser or editor-only content model should be introduced.

